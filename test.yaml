---
schemaVersion: 2.0.0
commandTests:
  - name: Ensure curl
    command: curl
    args:
      - --version
# fileExistenceTests:
#   - name: App working directory
#     path: /app
#     shouldExist: true
# metadataTest:
#   env:
#     - key: TEST_ENV
#       isRegex: true
#       value: \d+\.\d+\.\d+
